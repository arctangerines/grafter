* fix digdirncreate call
* Also fix the argument names, some arguments like "copy" or "exists" are inconsistent
* FIX ME THE NAME OF THE FUNCTIONS AND VARIABLES
* Option to copy an ID to a new path
* Option to update an ID?
* Add comments in the code indicating the type of flag eg if it changes main functionality or is supporting fucntionality
* organize the functions
* When integrating with scientific workflows we could additionally add the command line arguments used to generate a file.
* Change "Date" to "Timestamp"
* Hashing!!!
* We should have the option to deploy the document back to its original directory or to deploy it at the place we point at
* Compression?
